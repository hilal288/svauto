# Override configuration defaults by changing this file
#not_read_rc_defaults="YES"
if [ -z ${not_read_rc_defaults} ]
then
    for i in /usr/local/sandvine/etc/rc.conf.default*
    do
        if [ -f  $i ]
        then
            . $i
        fi
    done
fi
install_base=/usr/local/sandvine
# Change defaults below this line

SVSUPPORT=none@localhost

spb_spb_servers={{ hostvars[groups['svspb-servers'][0]]['ansible_eth1']['ipv4']['address'] }}

cluster_name="{{ cluster_name }}"
cluster_stat_name="{{ cluster_name }}"

# License Server
{% if release == 'prod' %}
#license_server_primary="{{ license_server }}"
{% endif %}
{% if release == 'dev' %}
license_server_primary="{{ license_server }}"
{% endif %}

SetPort fastpath 1-3 0000:00:05.0

SetPortRoleFunction 1-3 service

SetPortBridgeGroup 1-3 1
